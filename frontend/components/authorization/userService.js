require('angular')
.module('gather')
.factory('userService', ['BASEURL', '$http', 'authorizationService',
	function(BASEURL, $http, authorizationService) {
		var userService = {};
		userService.	
	}]);
